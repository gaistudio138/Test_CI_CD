graph TD
    subgraph "1. 開發者端 (Local/GitHub Repo)"
        A[Push Code] --> B{GitHub 偵測}
    end

    subgraph "2. 自動化劇本 (YAML Config)"
        B -->|觸發事件: Push/PR| C[".github/workflows/main.yml"]
        C --> D[定義任務: Jobs]
        D --> E[環境準備: runs-on ubuntu]
    end

    subgraph "3. 中央廚房 (GitHub Runner / CI)"
        E --> F[1. Checkout: 複製程式碼]
        F --> G["2. npm install (安裝依賴)"]
        G --> H["3. npm test (執行驗證)"]
    end

    subgraph "4. 專案大腦 (package.json)"
        G -.->|讀取| I["dependencies: 套件清單"]
        H -.->|呼叫| J["scripts: { 'test': 'jest' }"]
    end

    subgraph "5. 結果回饋"
        H -->|成功| K[✔️ CI Passed / 允許合併]
        H -->|失敗| L[❌ CI Failed / 阻止合併]
    end

    style I fill:#f9f,stroke:#333,stroke-width:2px
    style J fill:#f9f,stroke:#333,stroke-width:2px
    style C fill:#bbf,stroke:#333,stroke-width:2px